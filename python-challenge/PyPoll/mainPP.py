#import modules
import os
import csv

#Variable, List, and Dictionary definition
totalvotes = 0
cand_votes = {}
ind_totals = []
perc_won = []
winner = str


#finding location of data
location = "C:\\Users\\ZPurinton\\Documents\\Personal\\Data Boot Camp\\Homework\\python-challenge\\PyPoll\\Resources"
pollfile = os.path.join(location, "Unit 3 - Python_HomeWork_Instructions_PyPoll_Resources_election_data.csv")

def pollfun(candidate):
    for row in poll:
        if candidate == "Li"
            cand_votes
    
    
with open(pollfile, "r") as file:
    poll = csv.reader(file, delimiter = ",")
    
    for row in poll:
        if totalvotes == 0:
            totalvotes += 1
        else:
            totalvotes +=1
            
            
    print(totalvotes)
